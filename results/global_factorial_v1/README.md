# Global 纬度信息 × 模型类型析因实验结果

## 结论

四格实验均使用同一数据、同一 `original_ID` 切分和同一 test 行。D
（XGBoost + `sin(latitude)`）是数值最优，但相对冻结 A 的 joint R² 只提高
+0.004186，RMSE 只降低
0.244%。
它没有达到预先规定的 +0.01 joint R² / 1% RMSE 门槛，因此不进入 Windows
推理效率评价，也不替换冻结 global MLP。

## 四格结果

| 格 | 模型/输入 | joint R² | RMSE | MAE | macro-ID R² | macro-ID RMSE | vs A 胜率 |
|---|---|---:|---:|---:|---:|---:|---:|
| A | Frozen MLP core6 | 0.127398 | 0.194682 | 0.143305 | -0.302621 | 0.192287 | — |
| B | MLP lat7 | 0.130840 | 0.194291 | 0.142965 | -0.278165 | 0.192418 | 54.4% |
| C | XGBoost core6 | 0.127254 | 0.194698 | 0.143286 | -0.303854 | 0.192415 | 48.9% |
| D | XGBoost lat7 | 0.131584 | 0.194207 | 0.142873 | -0.238536 | 0.191979 | 55.2% |

## 析因效应

- MLP 下加入纬度（B − A）：joint R² +0.003442，
  RMSE -0.000391 m/s。
- 不含纬度时改用 XGBoost（C − A）：joint R²
  -0.000144，RMSE
  +0.000016 m/s。
- XGBoost 下加入纬度（D − C）：joint R²
  +0.004330，RMSE
  -0.000491 m/s。
- 含纬度时 XGBoost 相对 MLP（D − B）：joint R²
  +0.000744，RMSE
  -0.000085 m/s。
- 交互项：joint R²
  +0.000888。

## original_ID 直接比较

- D 相对 B：188 胜 /
  0 平 /
  178 负，胜率
  51.4%。
- D 相对 C：205 胜 /
  0 平 /
  161 负，胜率
  56.0%。

## 科学解释

纬度在 MLP 和 XGBoost 下都带来小幅、方向一致的改善；单独替换 XGBoost
没有收益。模型类型与纬度存在很弱的正交互，但幅度不足以解释当前 global
baseline 的主要误差。因此现有证据更支持“缺少粗粒度空间信息只解释少量误差，
模型类型不是主要瓶颈”，而不是立即替换冻结模型或进入部署。
