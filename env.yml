name: buoy-drifter
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  # PyTorch GPU (for WSL2+CUDA)
  - pytorch::pytorch
  - pytorch::torchvision
  - pytorch::torchaudio
  - pytorch::pytorch-cuda
  
  # Data processing
  - numpy
  - pandas
  - xarray
  - netcdf4
  - scipy
  - joblib
  - scikit-learn
  # XGBoost 析因实验固定使用 CUDA 12.9 构建；驱动向后兼容该 runtime。
  - conda-forge::xgboost=3.2.0=cuda129*
  
  # Visualization
  - matplotlib
  
  # Utilities
  - tqdm
  - python=3.11
  
pip:
  - pip
